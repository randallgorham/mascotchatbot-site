import { BlogPost, blogMeta } from "@/components/Blog";
export const metadata = blogMeta("capture-after-hours-leads");
export default function Page() { return <BlogPost slug="capture-after-hours-leads" />; }
